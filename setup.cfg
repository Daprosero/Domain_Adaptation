[metadata]
name = domain-adaptation
version = 0.1.0
description = Domain adaptation models and training utilities
long_description = file: README.md
long_description_content_type = text/markdown

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9

[options.packages.find]
where = src
